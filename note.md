# 思考内容
## .c文件
.c文件是c源代码文件，比如函数、变量、#include、注释等
## .o文件
.o文件是二进制文件，即目标文件，已经翻译成机器指令，但还没有完成最终链接，通常包含机器代码，全局变量、静态变量数据等，
但还无法直接运行
## 最终可执行文件
最终可执行文件本质是二进制程序，由链接器把一个或多个.o文件以及库链接后生成。
内容包含完整的机器指令，数据段、代码段以及程序入口点等。
##编译和链接的区别
gcc hello.c -o hello先经过gcc -c hello.c -o hello.o编译，再通过gcc hello.o -o hello链接生成可执行文件
因此编译和链接是两个具有先后顺序的不同步骤
编译主要做预处理处理#include、#define、条件编译等与进行语法分析，生成汇编代码产生.o目标文件
链接主要是针对一组目标文件和库，把他们组合成最终可执行文件，其过程是找出每个函数、全局变量的定义，把目标文件里的占位地址改成最终地址，把多个.o的代码段、数据段等合并，链接静态库.a或动态库.so，生成最终可执行文件
##为什么Make能做到增量构建
